import { View, Text } from 'react-native';

export default function RestaurantsScreen() {
  return (
    <View className="flex-1 items-center justify-center">
      <Text className="text-xl font-bold">Restaurants Screen (Main Layout)</Text>
    </View>
  );
}
