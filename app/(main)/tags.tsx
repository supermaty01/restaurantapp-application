import { View, Text } from 'react-native';

export default function TagsScreen() {
  return (
    <View className="flex-1 items-center justify-center bg-muted">
      <Text className="text-xl font-bold">Tags Screen (Main Layout)</Text>
    </View>
  );
}
